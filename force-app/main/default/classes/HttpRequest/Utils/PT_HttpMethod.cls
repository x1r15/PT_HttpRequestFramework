public enum PT_HttpMethod {
    //Values start with HTTP to provide consistency and avoid using forbidden keyword for DELETE
    HTTP_GET,
    HTTP_POST,
    HTTP_PUT,
    HTTP_DELETE,
    HTTP_HEAD,
    HTTP_CONNECT,
    HTTP_OPTIONS,
    HTTP_TRACE,
    HTTP_PATCH
}