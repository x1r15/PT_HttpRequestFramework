/**
 * Enum containing names of metadata records to avoid using magic strings in code.
 * Can be skipped if somebody prefers to actually work with strings...
 */
public enum ExampleRequestTypes {
    AlphaVantageRequest
}